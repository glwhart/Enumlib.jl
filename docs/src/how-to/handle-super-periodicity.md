# Handle super-periodicity

The `include_superperiodic` kwarg, what it changes, and when you'd want it set to `true` (training-data construction) vs the default `false` (canonical derivative structures).

!!! warning "Phase 13a placeholder"
    Content lands in **Phase 13c** of the v0.2 documentation rollout. This
    stub exists so the site builds end-to-end during infrastructure setup.
