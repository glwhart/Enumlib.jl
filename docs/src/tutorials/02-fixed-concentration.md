# Enumerating at a fixed concentration

Use the `Concentration_*` types and the `concentration` kwarg of `enumerate` to restrict the output to labelings with prescribed counts of each species.

!!! warning "Phase 13a placeholder"
    Content lands in **Phase 13d** of the v0.2 documentation rollout. This
    stub exists so the site builds end-to-end during infrastructure setup.
