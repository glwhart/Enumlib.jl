# Generating a DFT/MLIP training database

End-to-end workflow: enumerate → `write_enumeration_archive` → ship a tarball → calculator fills `energy_eV=` slots → `read_results` + `attach_results` → hand off to JuCE.jl. The pre-release narrative lives at `docs/notes/phase11-tutorial.md` and migrates here.

!!! warning "Phase 13a placeholder"
    Content lands in **Phase 13d** of the v0.2 documentation rollout. This
    stub exists so the site builds end-to-end during infrastructure setup.
